{
  "name": "obsidian-42go-share",
  "version": "1.0.0",
  "description": "42GoShare plugin for Obsidian - share a note with an ephemeral public link",
  "license": "MIT",
  "author": "Marco Pegoraro",
  "repository": {
    "type": "git",
    "url": "https://github.com/marcopeg/obsidian-42go-share"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "share",
    "link",
    "402go"
  ],
  "main": "src/main.ts",
  "scripts": {
    "dev": "concurrently \"node esbuild.config.mjs\" \"npx tailwindcss -i src/styles.css -o styles.css --watch --config tailwind.config.cjs\"",
    "build": "npx tailwindcss -i src/styles.css -o styles.css --config tailwind.config.cjs --minify && tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "tag:push": "git tag $(node -p \"require('./package.json').version\") && git push origin $(node -p \"require('./package.json').version\")",
    "tag:prune": "git tag -d $(node -p \"require('./package.json').version\") && git push --delete origin $(node -p \"require('./package.json').version\") || true"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9",
    "@typescript-eslint/eslint-plugin": "5.29.0",
    "@typescript-eslint/parser": "5.29.0",
    "autoprefixer": "^10.4.21",
    "builtin-modules": "3.3.0",
    "concurrently": "^9.2.1",
    "esbuild": "0.17.3",
    "obsidian": "latest",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "3.4.7",
    "tslib": "2.4.0",
    "typescript": "4.7.4"
  },
  "dependencies": {
    "@radix-ui/themes": "^3.2.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  }
}
